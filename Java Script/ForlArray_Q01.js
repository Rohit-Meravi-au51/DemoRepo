let rm = [23,43,56,78,11,43,67,89]; //Innitializing Array.
// Using For -Loop on Array(rm) to print all the elements of rm Array.
    for ( let i = 0 ; i < rm.length ; i++)
    {
        console.log(rm[i]);
    }