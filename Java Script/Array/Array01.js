let rm = [2,3,4,5,6,5,4,3,2];//initialization of the array.
//index =[0,1,2,3,4,5,6,7,8], index = n-1(here n is total number of elements in the array).
console.log("full array 01 content",rm);// to print the content of array.
//console.log("second",rm[4],rm[5],rm[6],rm[2]); //to access the array with index number.
//rm[5] = 9 //to update th array with the index number.
//console.log("third",rm[5]);
//console.log("lenght of array",rm.length); //to print the length(number of the elements) of array.
//rm[rm.length] = "rohit" //to insert the element at the last of the array.
//console.log("full array content",rm);// to print the content of array.
//rm[rm.length]=20; //add element at last using length.
rm.push(23,43,56,43,) //add multiple or single element at last of the array.
console.log("full array 02 content",rm);// to print the content of array.
rm.pop() //to remove the single element from the last.
console.log("full array 03 content",rm); //to print the content of array.
rm.unshift(40,50,60,70,20) //add multiple or single element at Begining of the array.
console.log("full array 04 content",rm);// to print the content of array.
rm.shift() //to remove the single element from the begining.
console.log("full array 05 content",rm); //to print the content of array.

// e= rm.indexOf(4);
// res = e * 5
// console.log(res);
