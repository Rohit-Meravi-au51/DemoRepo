a = 23
console.log(`hello World`);//This is Template Literal, using Tick (`) symbol.
console.log(`hello
my name\nis rohit  
age is ${a}.`); //This is Template Literal, using Tick (`) symbol and Variable. symbol /n is used to switch next line.
console.log(`there are ${(a<50)? "less than 50" : "more than" } buses`); //use of itenary Operator as code inside String Template.